// JavaScript Program to Find the Sum of Natural Numbers 
    
{
    let num = 12, sum = 0;

    for (let i = 1; i <= num; i++) {
        sum += i;
    }
    console.log(`sum of ${num} numbers : ${sum}`);
}