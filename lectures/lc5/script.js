{
  {
    // JavaScript Operations

    {
      // Equal to (==)
      // Strict equal to (===)
      // Not equal to (!=)
      // Strict Not equal to (!==)
      // Greather than(>)
      // Less than(<)
      // Greather than or equal to(>=)
      // Less than or equal to (<=)
      // let x = "13";
      // let y = 13;
      // console.log(typeof x);
      // console.log(typeof y);
      // console.log(x == y);
      // console.log(x === y);
      // console.log(x != y);
      // console.log(x !== y);
      // console.log(x > y);
      // console.log(typeof (x > y));
      // console.log(xx === yy);
      // console.log(typeof x > typeof y);
      // console.log(xx == yy);
      // console.log(x >= y);
      // console.log(x < y);
      // console.log(x <= y);
    }

    {
      // Ternary Operator (? :)
      // let x = false;
      // x ? console.log("True") : console.log("Flase");
    }

    {
      // Logical Operator
      // && (Logical END)
      // || (Logical OR)
      // ! (Logical NOT)
      // let x = true;
      // let y = false;
      // let z = true;
      // console.log(x && y);
      // console.log(x && z);
      // console.log(y && y);
      // console.log(x && y && z);
      // console.log(y || y || y);
      // console.log(!(x && y));
      // console.log(!(x && z));
      // console.log(!(x && y) || (!(x && x) && (x || z)) || (x && x));
      // console.log(!(x && y) && !!(x && x) && (x || z) && x && y);
    }

    {
      // String Operator

      // + (concatinating)
      // += (assign)

      // let str1 = "Hi ";
      // let str2 = "Hello Javascript!";

      // console.log(str1 + str2);
    }
  }
}
